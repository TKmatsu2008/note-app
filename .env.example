# このファイルをコピーして .env を作成する: cp .env.example .env

# ローカル開発用 DB (Docker Postgres / `docker compose up -d` で起動)
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/note_app?schema=public"

# --- 本番 (Supabase) ---
# Vercel の環境変数に設定する。ローカルでは設定不要。
# DATABASE_URL="postgresql://postgres.xxxxx:PASSWORD@aws-x-region.pooler.supabase.com:5432/postgres?pgbouncer=true"
